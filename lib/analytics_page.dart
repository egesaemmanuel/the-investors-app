import 'package:flutter/material.dart';

class AnalysticsPage extends StatelessWidget {
  const AnalysticsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        color: Colors.white,
        child: const Center(
            child: Text(
          'Analystic',
          style: TextStyle(color: Colors.black),
        )));
  }
}
